# Runtime dependencies (installed in isolated venv at first use)
# Location: ~/.kadas_ai_segmentation/venv_py3.x/
# Installation: Automatic via plugin UI ("Install Dependencies" button)

# Deep learning framework
torch>=2.0.0

# Segmentation model (version-dependent, see src/core/model_config.py)
# - Python 3.10+: sam2>=1.0 (SAM 2.1)
# - Python 3.9: segment-anything>=1.0 (SAM 1.0)
segment-anything>=1.0  # Fallback for older QGIS
sam2>=1.0  # For QGIS 3.34+ with Python 3.10+

# Geospatial and data processing
rasterio>=1.3.0
pandas>=1.3.0
numpy>=1.26.0,<2.0.0

# Security (optional, bundled via try/except in i18n.py)
defusedxml>=0.7.0
